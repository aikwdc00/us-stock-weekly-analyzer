services:
  - type: web
    name: us-stock-weekly-analyzer
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20.13.1
